## The first code in any programming language
print("Hello World! Hello Bangladesh!")

## Set default language as English if error message shows in other language
Sys.setenv(language = "en")

## Install any package(say tidyverse)
install.packages("tidyverse")

## Call the library to  use the function
library(tidyverse)

## Install multiple packages with one code
install.packages(c("nycflights13", "gapminder", "Lahman"))

## Get the updated package
tidyverse_update()
